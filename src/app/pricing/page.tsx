import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { ActionButton, PageHeader } from "@/components/proof-ui";

const plans = [{ name: "Free", price: "₹0", quota: "1 agent / 25 tests", reports: "Public report", badge: "—", support: "Community", cta: "Start free" }, { name: "Builder", price: "₹999 / mo", quota: "5 agents / 200 tests", reports: "Private + public", badge: "Included", support: "Priority", cta: "Start builder" }, { name: "Agency", price: "₹4,999 / mo", quota: "Unlimited / 500 tests", reports: "White-label reports", badge: "Included", support: "Dedicated", cta: "Start agency" }, { name: "One run", price: "₹299", quota: "1 agent / 1 run", reports: "Public report", badge: "Included", support: "Self-serve", cta: "Run once" }];
export default function PricingPage() { return <AppShell><div className="product-page"><PageHeader eyebrow="PRICING" title="Sell verified deployments" description="Choose the amount of verification evidence your team needs to put an agent in the world." /><div className="pricing-ledger"><div className="pricing-ledger__head"><span>PLAN</span><span>PRICE</span><span>QUOTA</span><span>REPORTS</span><span>BADGE</span><span>SUPPORT</span><span /></div>{plans.map((plan, index) => <div className={`pricing-ledger__row ${index === 1 ? "is-featured" : ""}`} key={plan.name}><strong>{plan.name}{index === 1 && <small>RECOMMENDED</small>}</strong><span className="pricing-ledger__price">{plan.price}</span><span>{plan.quota}</span><span>{plan.reports}</span><span>{plan.badge}</span><span>{plan.support}</span><ActionButton href="/agents/new" variant={index === 1 ? "primary" : "quiet"} icon={null}>{plan.cta}</ActionButton></div>)}</div><div className="pricing-note"><span className="eyebrow">DODO PAYMENTS / CHECKOUT READY</span><p>Subscriptions and pay-per-verification checkout are reserved for the production billing integration.</p></div><Link className="mono" href="/">← Return to AgentProof</Link></div></AppShell>; }
